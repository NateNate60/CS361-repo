"use strict"

function fetch_info () {
    alert("When the page loads, the program should send a GET request to an API endpoint set up by my partner's project instead of giving this placeholder message.")
}